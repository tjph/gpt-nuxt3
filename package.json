{
  "name": "gpt-nuxt3",
  "version": "0.1",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --hostname '0' --port=3000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.6.4",
    "nuxt": "^3.3.1"
  },
  "dependencies": {
    "openai": "^3.2.1"
  }
}
